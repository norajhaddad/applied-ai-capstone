# Build Log

A short entry per task: the brief, what Claude proposed, what I changed before
approving, how I verified it, and one thing I learned. The point is to capture
the decisions as they happen — code is cheap, the reasoning is the artifact.

## Task 1 — Scaffold + CLAUDE.md
- Brief: Create CLAUDE.md, .gitignore, requirements.txt (pytest), and pyproject.toml with `pythonpath = ["."]`. Verify a fresh session loads CLAUDE.md and `pytest` runs from the repo root collecting 0 tests with no import errors.
- What Claude proposed: CLAUDE.md + .gitignore were already committed earlier; add `requirements.txt` (pytest only — Flask comes in Task 5), `pyproject.toml` with `[tool.pytest.ini_options]` setting `pythonpath = ["."]` and `testpaths = ["tests"]`, a `tests/` dir so testpaths resolves, and start this BUILD_LOG.
- What I changed before approving: Approved the file plan as proposed; separately chose GitHub PR-per-task as the merge flow for the whole build.
- Verification: `python3 -m venv .venv && .venv/bin/pip install -r requirements.txt && .venv/bin/pytest` from the repo root → config loaded from pyproject.toml, `testpaths: tests` resolved, "collected 0 items / no tests ran", no import errors. (pytest exits code 5 = "no tests collected" — expected for an empty suite.)
- One thing I learned: pytest returns exit code 5 when it collects zero tests — that means "nothing to run," not "something broke," so an empty scaffold is healthy rather than failing. `pythonpath = ["."]` in pyproject.toml is what lets tests import top-level modules without packaging or installing the project.

## Task 2 — SUVAT solver
- Brief: Add kinematics.py with the five named, documented constant-acceleration functions. Verify pytest passes textbook cases (u=0, a=9.8, t=2 → v=19.6, s≈19.6) and edge cases (ValueError on negative sqrt domain, a=0).
- What Claude proposed: Five pure, type-hinted, documented functions with no Flask/I/O/globals — final_velocity (v=u+at), displacement (s=ut+½at²), displacement_from_final_velocity (s=vt−½at²), final_velocity_from_displacement (v=√(u²+2as)), displacement_from_velocities (s=½(u+v)t). The sqrt function raises ValueError on a negative radicand and returns the non-negative root; a=0 is left to reduce naturally to constant velocity. Plus tests/test_kinematics.py covering all of it.
- What I changed before approving: Approved as proposed.
- Verification: .venv/bin/pytest -v → 7 passed. The textbook scenario (u=0, a=9.8, t=2) was cross-checked across all five equations (each gives v=19.6 / s=19.6); the negative-domain case raises ValueError; a=0 returns constant-velocity values (v=u, s=u·t).
- One thing I learned: The five SUVAT equations are all rearrangements of the same motion, so a single scenario can cross-check every function — they must all agree on v=19.6 and s=19.6, which makes a tiny test suite surprisingly thorough. Only the v=√(...) form can fail on real inputs (negative radicand), which is why it's the one function that needs an explicit guard.

## Task 3 — Question generator
- Brief: Add a seedable generator that picks 3 known SUVAT values + a target and uses the solver to compute the correct answer, returning a question object. Verify: with a fixed seed, generate() returns a reproducible question whose answer matches the solver.
- What Claude proposed: generator.py with generate(seed) using a local random.Random instance; a frozen Question dataclass (prompt, given, target, unit, answer, equation); five templates mapping each kinematics solver to its target + 3 input variables; integer sampling ranges kept non-negative so the √ equation never errors; answer computed via the solver. Plus tests/test_generator.py for reproducibility, variety, answer-matches-solver, and shape.
- What I changed before approving: Asked for nicer unit display — acceleration shows the superscript glyph (m/s²) in prompts instead of m/s^2 — and had a test added to pin that.
- Verification: .venv/bin/pytest -v → 12 passed (5 new). generate(seed=42) is reproducible; across 20 seeds, re-running the recorded equation on the givens matches the stored answer; a hand-check (seed 1: v=18, a=2, t=5 → s=65 m via displacement_from_final_velocity) agrees.
- One thing I learned: Seeding a local random.Random(seed) instead of the module-level random keeps the generator pure and reproducible without touching global state — same seed gives an identical question, and unrelated code can't perturb it. And recording which equation produced each answer (the `equation` field) is what lets a test independently re-derive the answer, turning "trust me" into "check me."

## Task 4 — Answer checker
- Brief: Add a function that parses the user's text to a float and compares it to the correct answer within a tolerance, flagging unparseable input. Verify: "19.6"→correct, a near-miss within tolerance passes, a far value fails, "abc"/""→flagged invalid (no crash).
- What Claude proposed: checker.py with a Result enum (CORRECT/INCORRECT/INVALID), parse_float(text)→float|None (strips whitespace; returns None on empty/garbage/non-finite, never raises), and check_answer(text, correct, rel_tol=0.01, abs_tol=...) built on math.isclose. Plus tests/test_checker.py covering correct, near-miss, far, invalid, whitespace, and near-zero.
- What I changed before approving: Bumped the absolute tolerance floor from 1e-6 to 0.05, so one-decimal rounding of small answers still counts while the 1% relative band keeps governing large answers. Deferred unit-stripping (accepting "19.6 m/s").
- Verification: .venv/bin/pytest -q → 25 passed (13 new). "19.6"→CORRECT; "19.5" (~0.5%)→CORRECT; "25"→INCORRECT; "abc"/""/"   "/"1.2.3"→INVALID with no exception; "inf"/"nan"→INVALID; near-zero handled by the floor.
- One thing I learned: math.isclose accepts a value within max(rel_tol·|value|, abs_tol), so setting both gives a "within 1% OR within 0.05" rule for free — the relative term handles big answers, the absolute floor rescues small/near-zero ones where a percentage is unfairly tight. And returning an INVALID result instead of letting float() raise keeps bad input a normal, testable outcome rather than a crash the web layer has to defend against.

## Task 5 — Flask web shell
- Brief: Add Flask to requirements and create create_app() with a home route and base template. Verify: python app.py serves / (200) and the pure-logic tests still pass.
- What Claude proposed: Add Flask to requirements.txt; app.py with an app-factory create_app() exposing GET / → render_template("index.html"); templates/base.html (title/content blocks + header) and templates/index.html (welcome copy); a __main__ block serving on port 5000. tests/test_app.py driving the route with Flask's test client.
- What I changed before approving: Chose the forward-looking factory (option B) — wired SECRET_KEY (env var with a dev fallback) and the create_app(test_config=None) signature now, so Task 8's session/score tracking and test config slot in without refactoring the factory.
- Verification: .venv/bin/pytest -q → 28 passed (3 new). Real-server smoke test: launched python app.py and curled http://localhost:5000/ → HTTP 200 with the rendered title present; server log showed "Running on http://127.0.0.1:5000". Pure-logic tests unaffected.
- One thing I learned: The app-factory pattern (create_app() returning a fresh app, plus an optional test_config) is what makes the web layer testable without a running server — Flask's test_client() drives routes in-process, so "does / return 200?" becomes a unit test rather than a manual browser check. Configuring SECRET_KEY before sessions even exist means Task 8's session machinery won't force a factory rewrite.

## Task 6 — Serve a question (GET /quiz)
- Brief: Add GET /quiz that generates a question and renders it with an answer input. Verify: GET /quiz returns 200 and the page contains the question prompt and an answer field.
- What Claude proposed: A GET /quiz route calling generator.generate() and rendering templates/quiz.html (the prompt + a text input name="answer" + a Check button posting to /quiz, ready for Task 7); a "Start practicing" link added to the home page now that /quiz exists; tests for 200, prompt + answer-field presence, and session storage.
- What I changed before approving: Chose option (a) — stash the served question (including its answer) in the Flask session at GET time, so Task 7's POST can check against it server-side rather than via a guessable hidden field. Added a test asserting the question lands in the session.
- Verification: .venv/bin/pytest -q → 32 passed (4 new). Real-server smoke test: GET /quiz → HTTP 200 rendering e.g. "Given v = 21 m/s, a = 7 m/s², t = 4 s, find displacement s (m)." with the name="answer" input; the m/s² glyph renders in the browser.
- One thing I learned: Flask's default session is a signed cookie — tamper-proof but only base64-encoded, not encrypted — so anything stored there (the answer included) is readable by the client even though they can't alter it. For a no-stakes, no-database practice quiz that's a fine trade, but it's a sharp reminder that "in the session" is not "hidden from the user"; truly secret server-side state would need a server-side session store.

## Task 7 — Submit + check answer
- Brief: Add the submission route that runs the checker, shows correct/incorrect + the worked answer, validates input (reject empty/garbage, keep the typed value), and offers the next question. Verify: correct→"correct", wrong→"incorrect", empty input re-renders with an error.
- What Claude proposed: Make /quiz also handle POST — read the session's question, run check_answer, and either re-render the same question with an error (preserving the typed value) on INVALID, or render result.html (Correct!/Incorrect. + worked answer + Next link). New templates/result.html; quiz.html gains the error line and a value prefill.
- What I changed before approving: Chose option (b) — full worked solutions. Added a worked_solution() formatter in generator.py that renders each equation with the given numbers substituted (e.g. "s = ½·(u + v)·t = ½×(19 + 28)×7 = 164.5 m"), backed by five deterministic tests (one per equation).
- Verification: .venv/bin/pytest -q → 42 passed (10 new: 5 worked-solution + 5 submission). End-to-end: correct→"Correct!"; far value→"Incorrect"; ""/"abc"→re-render with "Please enter a number" and value="abc" preserved; the worked solution renders on the result page.
- One thing I learned: Handling GET and POST on one /quiz route keeps "serve question / grade answer" together, with the session as the bridge — the answer graded is the one that was actually served, not whatever the client posts. And the right way to show a validation error is to re-render the same template with extra error/typed context, not redirect: a redirect would throw away what the user typed, whereas re-rendering with value="{{ typed }}" keeps it.

## Task 8 — Score / session tracking
- Brief: Track attempts + correct count in the Flask session, show a running score, and add a reset action. Verify: two correct + one wrong shows 2/3 on the page; reset returns it to 0/0.
- What Claude proposed: Increment session["attempts"] on each graded answer and session["correct"] on correct ones; a POST /reset route clearing both and redirecting to /quiz; base.html header showing "Score: correct / attempts" (read from session) on every page, plus a Reset button. Tests for 2/3, reset→0/0, and invalid-not-counted.
- What I changed before approving: Chose option (a) — invalid/empty input does not count as an attempt (typos don't dent the score), and the score is shown plainly as "2 / 3" in the header.
- Verification: .venv/bin/pytest -q → 45 passed (3 new). Smoke test: 2 correct + 1 wrong → "Score: 2 / 3"; POST /reset → "Score: 0 / 0". Invalid submissions leave the count untouched.
- One thing I learned: Flask exposes the session object directly to Jinja, so a running total can live in the layout (base.html) and show on every page without each route passing it in — the views only write to the session, the template reads it. And putting the increment after the INVALID early-return is what makes "typos don't count" fall out for free, with no extra branching.

## Task 9 — Free-fall category
- Brief: Extend the generator with a free-fall category (a = g). Verify: free-fall questions use a ≈ 9.81 and their answers match the solver (pytest).
- What Claude proposed: Add G = 9.81 and a category argument to generate() (default "linear"); a category field on Question (default "linear"); a "free-fall" pool of the four acceleration-bearing equations with a fixed to G; tests that free-fall uses g, matches the solver, and excludes the no-a equation.
- What I changed before approving: Chose option (b) — free-fall questions are labeled "Free fall (g = 9.81 m/s²): ..." with gravity stated in the prefix and a omitted from the listed givens to avoid redundancy.
- Verification: .venv/bin/pytest -q → 49 passed (4 new). Free-fall samples render the label and use a=9.81 in the worked solution (e.g. "v = u + a·t = 23 + 9.81×6 = 81.86 m/s"); across 20 seeds the solver re-run matches the stored answer, and the no-acceleration equation never appears. (Noted quirk: displacement_from_final_velocity can give a negative displacement — mathematically valid, left as-is.)
- One thing I learned: Giving Question.category a default ("linear") let me add the field without breaking any existing Question(...) construction or older session cookies — a defaulted dataclass field is backward-compatible in a way a required one isn't. And keeping "linear" generation draw-for-draw identical (same rng call order) is what let the category feature land without disturbing the earlier reproducibility tests.

## Task 10 — Category filter UI
- Brief: Add an All/Linear/Free-fall selector that switches category via ?category=, reusing the tag-filter pattern. Verify: selecting a category serves only that category's questions; "All" mixes them.
- What Claude proposed: Teach generate() an "all" category (resolves to a random concrete one via the seeded rng); GET /quiz reads ?category= (default "all", unknown→"all"), stores the filter in session, and generates accordingly; POST + result read the filter back so Next/error stay in-category. A tag-style selector in quiz.html (All | Linear | Free-fall, active one bold) and a category-carrying Next link. Tests for per-category serving, all-mixes, selector links, and bogus input.
- What I changed before approving: Chose option (a) — tag-style text links (no JS, shareable ?category= URLs) — and kept the score running across category switches rather than resetting on change.
- Verification: .venv/bin/pytest -q → 54 passed (5 new). Real server: /quiz?category=linear → "Given ... a = 2 m/s² ..."; /quiz?category=free-fall → "Free fall (g = 9.81 m/s²): ..."; selector links render; ?category=all mixes both over many loads; bogus category → 200. Had to pin one older test to ?category=linear because the default is now the mixed "all" (free-fall prompts read "given", not "Given").
- One thing I learned: Resolving "all" to a concrete category inside generate() (not in the route) keeps the Question self-describing — it always carries the real category it belongs to, which is exactly what the scorer and worked-solution display want; the route only deals with the user's filter choice. And storing that filter in the session, separate from the question, is what lets "Next question" and error re-renders stay in the chosen category without threading it through every URL.

## Task 11 — Polish + README
- Brief: Add a results/score screen, basic styling, and a README with setup/run/test steps. Verify: the README steps work from a clean clone + fresh .venv.
- What Claude proposed: A vanilla static/style.css linked from base.html (container, score badge, pill-style category links, buttons, green/red verdicts); the home page turned into a score/landing screen (answered / correct / percentage, Start button, category links); a README with requirements, setup, run, test, and a project map.
- What I changed before approving: Chose option (a) — the home page doubles as the score/landing screen rather than adding a separate /score route. Also added two presentation tests (home summary present, /static/style.css served).
- Verification: .venv/bin/pytest -q → 56 passed (2 new). README steps re-run in a throwaway copy with a brand-new .venv: pip install + pytest (56 passed) + python app.py serving / , /quiz, /quiz?category=free-fall, and /static/style.css all 200; home renders the score summary.
- One thing I learned: Running the README in a clean copy with a fresh venv is the only way to actually trust it — it's the check that catches a forgotten dependency or a file that's present locally but never committed (git status confirmed README.md and static/ were untracked-but-present, so a real clone would have needed them added). And Flask serves static/ with zero config, so wiring the stylesheet was just url_for('static', filename=...) — no route required.

## CSS refresh — professional semantic restyle (follow-up to Task 11)
- Brief: Make the look professional and review-grade: the whole page fixed at max-width 700px, semantic HTML5 with CSS classes, and accessible (agents will scrutinize it). No logic changes.
- What Claude proposed: Restructure base/index/quiz/result into semantic landmarks (header, main, nav, section, footer) styled by meaningful class names; rewrite static/style.css as a neutral slate + indigo light theme — 700px centered page, surface cards, pill category nav, focus-visible rings; add accessibility (label/for + id, aria-live status/alert regions, aria-current on the active pill, aria-label nav). Preserve every string/attribute the tests assert.
- What I changed before approving: Chose the neutral slate + indigo palette, light mode only. Added three tests pinning the semantics (landmarks present, input has an associated label, verdict is an aria-live status).
- Verification: .venv/bin/pytest -q → 59 passed (3 new; all prior markers intact). Render checks confirmed <main>/<header>/<footer>, nav aria-label, for/id label pairing, role="status" verdict, role="alert" error, aria-current active pill, the 700px container and focus-visible in the CSS, and no inline style attributes anywhere.
- One thing I learned: Keeping every string/attribute the tests assert (the "2 / 3" score, name="answer", "Correct!", the category= links) while completely rewriting the markup turned the existing suite into a safety net for a pure-presentation change — I could restructure freely and instantly see whether I'd broken a contract. And semantic HTML/accessibility isn't decoration: a label tied to its input by for/id and an aria-live verdict are what separate "looks done" from "passes review."

## Task 12 (stretch) — Projectile motion
- Brief: Add a projectile category (launch angle; components: range / max height / time-of-flight) to the generator and solver. Verify: pytest cases match known projectile results (e.g. 45° gives max range) within tolerance.
- What Claude proposed: Three pure projectile functions in kinematics.py (range, max height, time-of-flight; level ground, g = 9.81); a "projectile" generator category that samples launch speed + angle and computes via the solver, with its own prompt and worked-solution templates; full UI integration — a Projectile pill in the selector, ?category=projectile, and inclusion in the "all" mix.
- What I changed before approving: Chose option (a) — full integration (solver + generator + 4th selector pill + included in "All"), rather than solver/tests only.
- Verification: .venv/bin/pytest -q → 68 passed (9 new). Solver: 45° maximizes range (40.77 vs 35.31 at 30°/60°), complementary angles tie, and u=10/45° matches known values (R≈10.19 m, T≈1.44 s, H≈2.55 m). Generator: projectile answers match the solver across 20 seeds, prompts state θ and g, worked solutions render (e.g. "H = (u·sinθ)²/(2g) = (24·sin21°)²/(2×9.81) = 3.77 m"). UI: ?category=projectile serves only projectile.
- One thing I learned: Projectile motion doesn't fit the SUVAT (s,u,v,a,t) variable scheme — its givens are a speed and an angle, and the trig means you can't show the arithmetic the way a linear substitution does. Branching the generator into a separate _generate_projectile, rather than forcing it into the shared equation table, kept both paths simple; and passing g into every worked-solution format call (harmless for templates that ignore it) let one function serve both families. Complementary angles giving equal range (30°/60°) is a neat, cheap correctness check that needs no reference value.

## Hardening — commercial-grade error handling + defensive guards (follow-up)
- Brief: Make the app commercial grade — nothing crashes with a raw stack trace; defensive coding with friendly messages; cleanup and comments.
- What Claude proposed: Friendly 404 / 405 / other-HTTP and catch-all error pages (templates/error.html) with the real cause logged server-side; defensive guards (regenerate on a malformed session instead of 500-ing, cap the answer length); cleanup + clarifying comments; README and dev-server notes.
- What I changed before approving: Chose "pragmatic + strict config" — on top of the pragmatic hardening, added fail-fast on the insecure default SECRET_KEY outside debug/testing (refuse to start) and a /healthz JSON health-check endpoint.
- Verification: .venv/bin/pytest -q → 76 passed (8 new). Real server: /nope → friendly 404, GET /reset → 405, /healthz → {"status":"ok"} 200. create_app() with no SECRET_KEY and not debug/testing raises RuntimeError; a malformed session payload redirects to a fresh quiz instead of 500-ing; a 5000-char answer is capped and handled normally.
- One thing I learned: Flask resolves error handlers by exception specificity, so registering 404/405 alongside a broad HTTPException handler and an Exception catch-all gives layered fallbacks with no conflict — the specific ones win, and the catch-all only sees genuinely unexpected errors (and only when PROPAGATE_EXCEPTIONS is off, which is exactly why TESTING lets exceptions surface loudly in tests). And failing fast at create_app time on the default SECRET_KEY converts a silent security mistake into an un-ignorable startup error.

## Answer field — numeric-only input + length cap (follow-up)
- Brief: The answer box accepted any text (a huge number just showed "incorrect"). Make the field itself accept only valid answers (integers, decimals, negatives) and cap its length, without misleading the user.
- What Claude proposed: Keep the input as type=text (so maxlength applies and the server's invalid-input path stays testable) and add HTML5 validation — maxlength=64 (matches _MAX_ANSWER_LEN), a numeric pattern, required, and a title hint. The server's parse_float / 64-char cap stay as the backstop. Add an attribute test.
- What I changed before approving: Required the field to reject invalid input in the browser (not only server-side) and to cap length — which ruled out type=number (it ignores maxlength and wouldn't limit a huge all-digit number), landing on type=text + pattern + required + maxlength.
- Verification: .venv/bin/pytest -q → 77 passed (1 new). /quiz renders the input with pattern, maxlength="64", and required; a valid answer still grades "Correct!". Manual: letters/empty are blocked at submit with a browser message; -12.5 and 164.5 are accepted; a pasted value stops at 64 characters.
- One thing I learned: HTML constraints are a UX nicety, not a security boundary — pattern/required/maxlength gate the browser form, but a crafted POST sidesteps them, so the server's parse_float and length cap must stay as the real gate. And maxlength simply doesn't apply to type=number, so wanting both "numbers only" and a length cap forces a text input with a pattern rather than a number input.
